# PUBLIC_INTERFACE
# Example environment file for the React frontend.
# REACT_APP_API_BASE_URL sets the backend API base URL.
# If omitted, the app will fallback to http://localhost:3001 for local development.
REACT_APP_API_BASE_URL=http://localhost:3001

# When "true", the app uses in-memory mock API instead of calling the backend.
REACT_APP_USE_MOCKS=false

# Default page size for devices pagination.
REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10

# Optional: disable React StrictMode double-invocation in dev
# REACT_APP_DISABLE_STRICT_MODE=false
