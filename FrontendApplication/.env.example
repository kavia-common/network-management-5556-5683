# Frontend React App Environment Example
# Copy this file to:
# - .env.development.local (preferred during development), or
# - .env
#
# After updating env files, restart the frontend dev server/preview.

# Base URL for the backend API.
# For local dev (frontend:3000, backend:3001) use:
# REACT_APP_API_BASE_URL=http://localhost:3001
# For HTTPS previews, set to the matching HTTPS backend origin and host:
# Example (update host to current preview host if it changes):
# REACT_APP_API_BASE_URL=https://vscode-internal-28439-beta.beta01.cloud.kavia.ai:3001
REACT_APP_API_BASE_URL=http://localhost:3001

# Use in-memory mocks instead of calling the backend
REACT_APP_USE_MOCKS=false

# Default page size for device list pagination
REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10

# Performance: disable sourcemaps in development to speed up rebuilds
GENERATE_SOURCEMAP=false

# Optional: disable StrictMode double invocation during development only
# REACT_APP_DISABLE_STRICT_MODE=true
