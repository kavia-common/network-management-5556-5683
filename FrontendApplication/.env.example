# React Frontend Environment Example
# Copy this file to .env (or .env.development.local) and adjust as needed.
# IMPORTANT: When the frontend is served over HTTPS, set an HTTPS API base URL to avoid mixed-content errors.

# Base URL for the backend API. Examples:
# - Local dev (HTTP): http://localhost:3001
# - Hosted preview (HTTPS; use your exact backend origin host and port):
#   https://vscode-internal-26250-beta.beta01.cloud.kavia.ai:3001
REACT_APP_API_BASE_URL=https://vscode-internal-26250-beta.beta01.cloud.kavia.ai:3001

# Use in-memory mock API instead of real backend (true/false)
REACT_APP_USE_MOCKS=false

# Default page size for pagination in the Devices table
REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10

# Dev-only: disable StrictMode double-invocation effects (true/false)
# REACT_APP_DISABLE_STRICT_MODE=true

# Performance (build-time)
# GENERATE_SOURCEMAP=false
