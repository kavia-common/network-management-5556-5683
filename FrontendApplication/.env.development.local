# Frontend Application - Development environment overrides
# API base URL for the backend service (HTTPS preview)
REACT_APP_API_BASE_URL=https://vscode-internal-26250-beta.beta01.cloud.kavia.ai:3001

# Optional: disable React StrictMode in dev (keeps default behavior if omitted)
# REACT_APP_DISABLE_STRICT_MODE=false

# Optional: enable mock API (UI-only development)
# REACT_APP_USE_MOCKS=false

# Optional: default page size for devices list
# REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10

# Speed up dev builds by disabling sourcemaps
GENERATE_SOURCEMAP=false
