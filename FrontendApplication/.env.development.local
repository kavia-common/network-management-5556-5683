# Preferred dev env file for local preview. Adjust API base to your backend origin.
# If your frontend preview is HTTPS at port 3000, set API base to matching HTTPS backend origin.
# Backend base URL. For local dev typically http://localhost:3001
# For this workspace's running backend preview, set to the provided origin.
REACT_APP_API_BASE_URL=http://localhost:3001
# Enable mock API to run UI without backend (true/false)
REACT_APP_USE_MOCKS=false
# Default page size
REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10
# Optional: disable StrictMode double-invoke in dev
# REACT_APP_DISABLE_STRICT_MODE=true
# Faster rebuilds
GENERATE_SOURCEMAP=false
REACT_APP_USE_MOCKS=false
REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10
GENERATE_SOURCEMAP=false
