# Frontend local/preview environment

# IMPORTANT: Match the protocol/host of the preview environment to avoid mixed-content and CORS issues.
# Set API base to the BackendAPIService preview origin.
# Update this value if your preview host changes.
REACT_APP_API_BASE_URL=https://vscode-internal-37855-beta.beta01.cloud.kavia.ai:3001

# Disable source maps in dev for faster preview rebuilds
GENERATE_SOURCEMAP=false

# Optional: toggle mock API for UI-only testing (set to true to bypass backend)
# REACT_APP_USE_MOCKS=false

# Default pagination size
# REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10
