# Frontend environment for current preview alignment
# Ensures the React app calls the correct backend origin to avoid mixed content and CORS issues.
REACT_APP_API_BASE_URL=https://vscode-internal-28439-beta.beta01.cloud.kavia.ai:3001

# Optional defaults (documented here for convenience)
# REACT_APP_USE_MOCKS=false
# REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10

# Faster rebuilds in dev
GENERATE_SOURCEMAP=false
