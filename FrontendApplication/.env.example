# Example environment configuration for the React frontend.
# Copy this file to `.env` (or `.env.development.local`) and adjust values as needed.

# PUBLIC_INTERFACE
# REACT_APP_API_BASE_URL
# Base URL for the backend API. Use the full origin with protocol to avoid mixed-content issues
# when the frontend is served over HTTPS.
# Examples:
#   - Local dev (backend on 3001): http://localhost:3001
#   - Preview/remote backend (HTTPS): https://your-preview-host:3001
# Notes:
#   - The client normalizes trailing slashes.
#   - If you set "localhost:3001" without protocol, the client will normalize it to "http://localhost:3001".
#   - After changing this file, you must restart the frontend dev server/preview.
REACT_APP_API_BASE_URL=https://kavia-alb-417f4a5e-113462135.backend.kavia.app/

# Optional:
# REACT_APP_USE_MOCKS=false
# REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10
