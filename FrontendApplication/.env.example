# PUBLIC_INTERFACE
# This example lists environment variables the frontend supports.
# Copy to .env (for global) or .env.development.local (for local dev only) and adjust.

# Base URL for the backend API
# Example: http://localhost:3001
REACT_APP_API_BASE_URL=

# Use mock API instead of backend. "true" or "false".
REACT_APP_USE_MOCKS=false

# Default page size for pagination
REACT_APP_PAGINATION_PAGE_SIZE_DEFAULT=10

# Performance: disable sourcemaps to speed up dev builds (optional)
# GENERATE_SOURCEMAP=false

# Dev server port
PORT=3000
