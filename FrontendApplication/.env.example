# Environment variables for the FrontendApplication (Create React App)
# For CRA, variables must be prefixed with REACT_APP_
# For Vite (if migrated later), create a .env with VITE_ equivalents.

# Backend API base URL (e.g., http://localhost:5000 or deployed URL)
REACT_APP_API_BASE_URL=http://localhost:5000

# Toggle mock mode to use in-memory API instead of backend.
# Accepts "true" or "false"
REACT_APP_USE_MOCKS=true
