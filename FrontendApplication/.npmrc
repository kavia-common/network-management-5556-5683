save-exact=true
fund=false
audit=false
progress=false
loglevel=error
# Prefer reusing cache if available
prefer-offline=true
# For Node 16+ / npm 8+, peer deps are handled; avoid legacy mode
legacy-peer-deps=false
