# Backend API Service environment variables (example)

# PUBLIC_INTERFACE
# FRONTEND_APP_ORIGIN
# Exact origin (scheme + host + port) of the frontend allowed to access this backend.
# Do NOT include wildcards. Example values:
#  - http://localhost:3000
#  - https://<your-preview-host>:3000
FRONTEND_APP_ORIGIN=http://localhost:3000

# Optional: Mongo connection examples (adjust per your env)
# MONGO_URI=mongodb://localhost:27017/network_devices
